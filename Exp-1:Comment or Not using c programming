#include<stdio.h>
#include<string.h>
int main()
{
	char a[30],length,i;
	printf("Enter the text:");
	scanf("%s",a);
	int len = strlen(a);
	if (a[0]=='/' && a[1]=='/' || a[len-1]=='*' && a[len]=='/')
	printf("it is comment");
	else
	printf("it is not a comment");
	return 0;
}
